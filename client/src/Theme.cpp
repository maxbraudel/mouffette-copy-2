#include "Theme.h"

QColor gOverlayBackgroundColor = QColor(0, 0, 0, 160);        // default semi-opaque dark
QColor gOverlayActiveBackgroundColor = QColor(74, 144, 226, 180); // default accent tint
int    gOverlayCornerRadiusPx = 8;
